import { StyleSheet } from "react-native"

const styles = StyleSheet.create ({
    container: {
        margin: 5,
        padding: 10,
        borderRadius: 20,
        maxWidth: '80%',
    },

    time: {
        color: 'gray',
        alignSelf: 'flex-end',
    },
})

export default styles